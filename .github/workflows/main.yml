name: "emsdk"
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: mymindstorm/setup-emsdk@v11

      - name: EnvCheckPwd
        run: echo "pwd: $(pwd)"

      - name: EnvCheckLs
        run: ls -al

      - name: Compile
        run: ./compile.sh
